Syntax : 
./sythesize_yosys.sh . <top module> 

Ex : 
./sythesize_yosys.sh . histogram_equalizer_core

1. Need to categorize rtl into a separate source directory (soft link) and need a synth directory 

